const initialState = {
  id: "",
  username: "",
  password: "",
  accessType: "",
  tickets: []
}

export const TicketReducer = (state = initialState, action) => {
  switch(action.type) {   
    default :
      return state
  }
}

